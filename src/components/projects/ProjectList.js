import React from 'react';

const ProjectList = ({ projects }) => {
    console.log(`projects: `, projects);
  return <section className="project__wrapper">Project List</section>;
};
export default ProjectList;
